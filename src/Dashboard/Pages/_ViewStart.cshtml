@{
    // Login sayfası hariç diğer tüm sayfalarda layout kullan
    var pageName = ViewContext.RouteData.Values["page"]?.ToString();
    if (pageName != "/Login")
    {
        Layout = "_Layout";
    }
}
